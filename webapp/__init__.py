"""Web UI package for Echo Loop Generator.

A thin FastAPI layer that reuses the existing CLI pipeline (``run_text_mode`` /
``run_interview_mode`` from ``main.py``) so the generator can be self-hosted and
driven from a phone browser.
"""
