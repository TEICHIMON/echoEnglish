# echoEnglish

## Audio prompt 同步规则（硬性要求）

凡是修改/新增任何关于 audio 生成 prompt 的说明，都必须同步更新 interview-notes 项目的 audio 命令。涉及范围包括但不限于：

- `interviewPrompt.md`（mock interview 脚本生成 prompt）
- webapp AI prompt helper 里的 prompt 文案（`webapp/static/index.html`、`webapp/static/app.js`）
- 影响脚本格式约定的 parser 行为（如 `Q:/A:` 前缀、`|||` 分隔、三语稿列序 EN→JA→ZH、注音假名剥离规则）

同步目标（两处哪个涉及改哪个）：

- `/Users/sudami/WebstormProjects/resume20260521/interview-notes/.claude/commands/audio.md`
- 该项目 `CLAUDE.md` 中的 `## AUDIO contract` 章节

改完后必须在回复中报告同步了哪些文件。

## TTS / LRC 对齐规则（硬性要求）

- 最终字幕分段必须在 TTS 之前完成。一条最终字幕必须一对一对应一个 target 音频片段；
  需要 native TTS 时，也必须一对一对应一个 native 音频片段。
- TTS 字幕的起止时间只能来自解码后的真实音频时长。严禁先生成多句/整段 TTS，再按字符数、
  单词数、平均语速或比例估算句内时间轴。
- 混合原音与 TTS 时，每个 segment 必须保留来源（原音切片或 TTS）和真实毫秒长度；任一
  segment 缺少真实边界时必须失败，不能降级为估算时间。
- 任务辅助脚本必须复用 `audio.tts_generator`、`audio.timing`、assembler 和 LRC writer 的
  segment 级接口，不能在输出目录中另写一套近似 Echo/LRC 实现作为最终产物管线。
- 生成后必须验证：segment 数量一一对应；LRC 累计时长使用实际 target/native 长度；最终音频
  可完整解码；任何 `estimated` timing provenance 都视为失败。

## 对白流水线：怎么跑

**完整命令序列见 `docs/dialogue_runbook.md`。** 要跑一部片子就照那份走，
不要自己拼命令 —— 里面记着几个只有踩过才知道的坑（8000 端口要单独验、
产物已存在时 `echo_pipeline` 会静默跳过、ssh 后台任务会挂住通道）。

设计理由见 `docs/dialogue_pipeline_claude_v2.md`。下面几节是**不要回退的结论**。

## 运行环境：GPU 活跑在 WSL2 里，不是原生 Windows

转写 / 人声分离等 GPU 任务都在一台 Windows 笔记本**内的 WSL2 (Ubuntu 24.04)** 里跑，
不是原生 Windows 上的 Python。设计文档里凡是写「Windows GPU 服务」的地方，指的都是它。

```bash
ssh -i ~/.ssh/mdreader_deploy -p 2222 sudami@192.168.31.50
```

- **RTX 4060 Laptop，只有 8GB 显存** —— 选型的首要约束
- whisper 服务在 `~/transcribe-server`（端口 8000），由 Windows 桌面的两个 bat 启停，
  **不要从 WSL 内部动它**
- `.wslconfig` 是 `networkingMode=mirrored`，WSL 里绑的端口局域网直接可达，不需要端口转发
- `nvidia-smi` 不在 PATH，要用 `/usr/lib/wsl/lib/nvidia-smi`
- sudo 需要密码，SSH 会话跑不了 —— 需要 sudo 的命令交给用户自己敲

**完整运维手册：`~/.claude/gpu-box.md`**（硬件容量、五个必知的坑、装 ollama 的步骤、
给 agent 的行为约定）。要在那台机器上做任何事，先读它。

### 人声分离已选定 htdemucs

2026-08-26 用同一段电影素材实测了四个方案，**按速度选 htdemucs**：30 分钟音频约 110 秒
处理完（TIGER-DnR 要 46 分钟）。

TIGER-DnR 的对白轨更干净 —— 它是唯一按「对白/音乐/音效」三类拆的模型，而 demucs 和
BS-RoFormer 都是*音乐*分离模型，只有「人声 vs 伴奏」两个桶，电影里的拳头声无处可放会
漏进人声轨。但下游剪辑真正需要的是语音段与空白段的电平落差，两者是 +11.5 vs +12.0 dB，
都够用（实测差 0.5 dB）。所以取速度。TIGER 保留为已验证的升级路径。

**别再评估 SAM-Audio**：依赖地狱，且 8GB 显存只够跑 `sam-audio-small`，
拿不到 `base` 的质量优势。详细理由在 `~/.claude/gpu-box.md`。

### 已知代价：动漫的 OP/ED 歌声会留在对白轨

2026-08-29 用《命运石之门 剧场版》实测：片头曲那 134 秒里，**81.5 秒（61%）被当成
对白保留了下来**。

原因在 demucs 的分类方式，不是 bug：它只分「人声 vs 伴奏」，而唱歌就是人声。
分离后的 vocals 轨里有歌声 → VAD 在那条轨上判定「有人在说话」→ 剪辑保留。

**这条已经评估过并接受了**，不要再去查：

- 真人电影影响小（配乐通常是器乐）
- 动漫每部大概几分钟（OP + ED + 插入曲）
- 剪辑后是连续对白，听到一段歌跳过即可

想彻底解决只有换 TIGER-DnR —— 它有独立的「音乐」类别，唱歌会进音乐轨。
代价是一部片子从 4 分钟变成 3 小时。取舍见上面的分离模型对比表。

**别为这个往流水线里加机械修补。** 曾经试过两条弯路，都被推翻了：

1. 「按汉字相邻检测断词」—— 日语句尾句首本来就常是汉字，误报泛滥，
   而真正的平假名断词（な|ぜ）它看不见
2. 「修复跨越巨大静音的词时间戳」—— 那个「异常间隙」本身就是被保留的歌声，
   修的是错误诊断的症状

教训：拿到现象先验证原因，别假设一个就动手修。上面那 81.5 秒只用一条命令就能量出来，
却是在写了两百行修补代码之后才去量的。


### 哪一步在哪台机器上跑（硬性分工）

> **GPU 机只做需要 GPU 的两件事：人声分离、whisper 转写。其余全部在 Mac。**

```
Mac  ──原音──────────▶  GPU 机 : htdemucs 分离
Mac  ◀────────stem────  GPU 机
Mac  : VAD 找边界 → 剪辑                      ← 剪辑必须在转写之前
Mac  ──剪辑后的音频────▶  GPU 机 : whisper 转写
Mac  ◀────result.json──  GPU 机
Mac  : 切块 → 分句 → 翻译 → Echo → 同步
```

理由（三条，按重要性）：

1. **剪辑那几步根本不需要 GPU** —— VAD 用的 silero 是 1MB 级的 CPU 模型，
   剪辑是 pydub/ffmpeg 的纯 I/O，电平判定是 numpy 上的滚动最小值。
2. **产物的归宿在 Mac** —— 剪辑输出直接喂给 `main.py --scan`，而 Echo 的 config、
   Google TTS 凭据、iCloud 同步全在 Mac 上。放 GPU 机就是绕一圈再传回来。
3. **避免环境双份** —— `tools/dialogue_cut.py` 依赖 Mac 上 `echo_env` 的
   faster-whisper 和 pydub。在两台机器各维护一份，正是 `~/.config/echo/paths.env`
   那个教训的复现方式（路径复制进 4 个脚本，迁移时漏改 3 个，静默失效很久）。

附带好处：那台笔记本会关机（实测断过），而只要几十秒的步骤不该依赖一台可能没开的机器。

**代价：变成两次往返。** 转写排到剪辑之后（决策 1.1），所以音频要上去两趟。传输量反而
小了（第二趟传的是剪掉静音之后的音频），但流程上多一次等待，而且**中间那台机器不能关**。


### 转写喂的是分离后的 stem，不是原音（2026-08-30 实测改定）

原来的设计是转写原混音，理由写的是「分离伪影伤识别」。**那条理由从来没测过，实测不成立。**

《搏击俱乐部》6 段各 6 分钟（共 36 分钟），拿 mkv 内封的人工英文字幕当参照：
词错误率原混音 24.23% / stem 24.71%，差 0.48 个百分点；按字幕行 bootstrap 8000 次，
95% 区间 [−1.25, +2.35] 含 0；逐行胜负 69:64，符号检验 p = 1.0。词时间戳精度也没差别
（绝对误差中位数 150ms vs 190ms）。**两者没有可测量的差别。**

既然质量上是平的，就选对流水线更顺的那个：转写 stem，让 whisper 的词时间戳和剪辑用的
VAD 落在同一条信号上。

**代价：分离从「可降级」变成硬依赖。** 分离失败不再剩一份字幕，整条任务作废。
所以「长音频切 15 分钟一段」（见上面第 ⑥ 条 OOM）从建议升级成必须。
另外分离偶尔会把一段话搅坏 —— 36 分钟里遇到 1 次；反方向也有 1 次原混音更差的。
两边都会局部翻车，这正是总体打平的原因。

> 顺带一条方法论：只用前 2 段素材时，结论是「stem 好 2.51 个百分点，95% 区间不含 0」。
> 补到 6 段后这个结论完全消失。素材不够时 bootstrap 照样会给「稳健」的假象。

### 剪辑规则：刀口只落在验证过的底噪里（2026-08-30 实测定）

**别再用「VAD 边界两侧各留固定 300ms」那套。** 实测 515 个语音段：VAD 给出的边界
落在声音**内部**，起点之前平均还有 50ms 声音（p90 226ms，最大 460ms）。清辅音本来
就是从几乎无声慢慢爬上来的，任何阈值都必然触发得晚。所以固定常数必然两头不讨好 ——
零留量削掉 92.4% 的词头，300ms 也还是会削一部分。

现在的规则（`tools/dialogue_cut.py`）：

```
对每个语音段之间的空隙：
  两边分别往中间走，找到「连续 50ms 都不高于本地底噪 6dB」的位置
  中间还有余量  -> 下刀，两边各留 30ms
  够不到底噪    -> 不剪，两段连着保留
本地底噪 = 能量的 ±2 秒滚动最小值（不是全局阈值 —— 分离残留在整片里并不均匀）
```

句间空白 600ms → 60ms。测试验的是两条不变量：每一刀都在验证过的底噪内部；
每段语音一帧不少。

**压缩率因此下降，而且随素材变**：英语电影剪掉约 23%，对白密集的动漫只剪掉 11.5%
（63% 的边界够不到底噪，不剪）。**这是接受的代价 —— 目标是「没有空白」，不是
「尽可能短」。不要为了压缩率去放松底噪判据，那等于把削词头重新买回来。**

剪掉静音不伤转写（实测）：WER 24.71% → 23.29%，bootstrap 区间含 0，
词数 4733 → 4710 基本不变。

### Echo 切 target clip：结束时间逐句从音频量（2026-08-30 实测定）

LRC 每行只有开始时间，`parse_lrc` 只能把 `end_ms` 推成「下一行的开始时间」
（`parser/lrc_parser.py:265`）。照着这个值直接切会同时犯两个错：

- 两句连着说时这一刀砍在词中间，上一句尾巴被削掉 —— 实测 47/100 的 clip 结尾还在说话
- 一句话后跟长停顿时整段停顿被算进 clip —— 尾部空转最长 3.43 秒，还和
  `after_first_target: 0.8s` 叠加，造成停顿忽长忽短

现在 `audio/splitter.py` 逐句从音频量：找到本行最后一个有声帧，往后留 200ms
（`TAIL_PAD_MS`）；边界处还在说话就往后走到这段语流结束，但最多越过下一行起点
400ms（`MAX_TAIL_EXTEND_MS`，防止连续语流里吞掉下一句）。判据是**相对**的：
高于「附近 ±2 秒峰值 − 25dB」才算有声。**`start_ms` 一个字节不动**，所以下一行
不受影响；输出 LRC 本来就按 clip 实际解码长度重建（`export/lrc_writer.py:69`），
自动对齐。

实测（同一条音频、同一批 segment，唯一变量是切法）：

| | 旧 | 新 |
|---|---|---|
| clip 结尾处有语音 | 47/100 | 16/100 |
| 尾部空转 p90 | 895ms | 200ms |
| 尾部空转 最长 | 3.43s | 0.21s |

剩下那 16 个不是尾巴被切，是 200ms 留量伸进了下一句开头（下一句在 200ms 内就开口）。

**三条已验证走不通的路，别再试：**

1. **换成 SRT 格式**并不解锁这件事 —— `extract_segment` 本来就不必用 `end_ms`。
   而且若 SRT 的结束时间取自 whisper 的词 `end`，那是**更差**的：实测系统性偏早
   120ms，81% 的句尾会被切掉，最多早 700ms。
2. **无条件给每个 clip 加尾部留量** —— 只有约一半的行结尾真被切了，另一半会把
   下一句开头拖进来。必须逐句判断。
3. **无条件加前置留量**（修 clip 开头用）—— 会把上一个词的尾巴拖进来，实测指标
   从 34.9% 恶化到 52%，whisper 首词识别率从 89% 掉到 73%。

**还没解决：clip 的开头。** 实测 47/100 的 clip 一开始就是语音。修法和尾巴不同
（要往回退到最近的静音），而且量过收益小得多（35% → 22%）。

**分离输出建议加 `--flac`**：无损，体积减半（30 分钟 303MB → 150MB）。
局域网上只省几秒，整片处理时省约 600MB 和半分钟传输。ffmpeg/pydub 原生读 flac，
下游无感。属于顺手不亏，不是性能优化。

